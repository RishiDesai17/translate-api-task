{
  "name": "translate-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "mocha --timeout 10000",
    "start": "set NODE_ENV=production&& node server.js || export NODE_ENV=production&& node server.js",
    "dev": "set NODE_ENV=development&& nodemon server.js || export NODE_ENV=development&& nodemon server.js",
    "pretest": "eslint --ignore-path .gitignore ."
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "dotenv": "^9.0.2",
    "express": "^4.17.1",
    "mysql2": "^2.2.5"
  },
  "devDependencies": {
    "chai": "^4.3.4",
    "chai-http": "^4.3.0",
    "eslint": "^7.26.0",
    "mocha": "^8.4.0",
    "nodemon": "^2.0.7"
  }
}
